# Reliability and Safety

- Domain models validate inputs.
- Reconciliation uses deterministic explicit references.
- `mock` is the safe, repeatable provider default.
- Low-confidence, critical, and human-review cases escalate.
- Every agent action creates an audit event.
- Evaluation uses fixed-seed synthetic ground truth and labels its limits.

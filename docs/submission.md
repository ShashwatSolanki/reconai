# Buildathon Submission

ReconAI reconciles 100 synthetic records, investigates only exceptions with an AI-compatible provider, applies bounded resolve/escalate guardrails, and emits an audit trail and verified report.

Demo: run `python scripts/run_controller_evaluation.py`; show the 96/4 reconciliation split, the high-confidence partial-settlement resolution, the three human escalations, and `reports/evaluation.md`.
